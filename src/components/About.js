import React from 'react';
import './About.css';

const About = () => (
  <div className="hero">
    <div className="hero-text">
      <p>This app is written using styled-components with React.</p>
    </div>
  </div>
);

export default About;